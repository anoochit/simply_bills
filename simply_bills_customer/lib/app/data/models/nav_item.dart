import 'package:flutter/material.dart';

class NavItem {
  String title;
  IconData icon;
  IconData selectedIcon;
  NavItem({
    required this.title,
    required this.icon,
    required this.selectedIcon,
  });
}
