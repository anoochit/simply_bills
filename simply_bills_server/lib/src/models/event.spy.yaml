class: Event
table: event
fields:
  title: String
  startDate: DateTime
  endDate: DateTime
  author: module:auth:UserInfo?,relation(onUpdate=SetNull, onDelete=SetNull)
  createdAt: DateTime
  publish: bool
