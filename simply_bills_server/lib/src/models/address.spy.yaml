class: Address
table: address
fields:
  uid: String
  address: String
  address2: String
  owners: List<UserAddress>?, relation(name=address_useraddress)
  invoices: List<Invoice>?, relation(name=address_invoice)
