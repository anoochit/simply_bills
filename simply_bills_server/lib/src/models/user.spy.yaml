class: User
table: user
fields:
  userInfo: module:auth:UserInfo?, relation(onUpdate=SetNull, onDelete=NoAction)
  name: String
  address: List<Address>?, relation
  telephone: String?
  email: String?
  scope: UserScope

indexes:
  user_library_unique_idx:
    fields: userInfoId
    unique: true
