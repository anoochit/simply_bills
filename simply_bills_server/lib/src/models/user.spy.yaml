class: User
table: user
fields:
  userInfo: module:auth:UserInfo?, relation(onUpdate=SetNull, onDelete=NoAction)
  name: String
  address: String
  tel: String
  scope: UserScope

indexes:
  user_library_unique_idx:
    fields: userInfoId
    unique: true
